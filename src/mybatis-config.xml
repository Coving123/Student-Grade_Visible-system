<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
	<!-- <settings>
		<setting name="cacheEnabled" value="true" />
		level 2 cache enabled
		<setting name="lazyLoadingEnabled" value="true" />
		enabled lazyloading
		<setting name="aggressiveLazyLoading" value="false" />
		disabled anti-lazyloading
	</settings> -->
	<typeAliases>
		<!-- <typeAlias alias="customer" type="com.learn.entity.Customer" /> -->
		<!-- it doesn't distinct case of letter -->
		<package name="com.learn.entity" />
		<!-- lowercase first letter class -->
	</typeAliases>
	<!-- <environments default="mysql">
		<environment id="mysql">
			<transactionManager type="JDBC"></transactionManager>
			<dataSource type="POOLED">
				<property name="driver" value="${jdbc.driver}" />
				<property name="url" value="${jdbc.url}" />
				<property name="username" value="${jdbc.username}" />
				<property name="password" value="${jdbc.password}" />
			</dataSource>
		</environment>
	</environments> -->
	<mappers>
		<!-- <mapper resource="com/learn/pojo/CustomerMapper.xml"></mapper> -->
		<!-- <mapper resource="com/learn/pojo/UserMapper.xml"></mapper> -->
		<!-- <mapper resource="com/learn/mapper/CustomerMapper.xml"></mapper> -->
		<!-- <mapper resource="com/learn/mapper/IDcardMapper.xml" />
		<mapper resource="com/learn/mapper/PersonMapper.xml" />
		<mapper resource="com/learn/mapper/UserMapper.xml" />
		<mapper resource="com/learn/mapper/OrdersMapper.xml" />
		<mapper resource="com/learn/mapper/ProductMapper.xml" /> -->
		<mapper resource="com/learn/entity/CustomerMapper.xml" />
	</mappers>
</configuration>